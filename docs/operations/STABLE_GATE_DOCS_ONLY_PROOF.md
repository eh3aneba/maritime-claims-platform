# Stable gate proof

This temporary proof change is intentionally documentation-only.

It exists to validate that protected-main aggregate checks remain present and green even when expensive browser, performance, and PostgreSQL concurrency workloads are not relevant to the pull request diff.
